<h1>ユーザー一覧</h1>
<p><%= notice %></p>

<div class="table">
  <table>
    <tr>
      <th><%= t('user_name') %></th>
      <th><%= t('email') %></th>
      <th><%= t('admin') %></th>
      <th><%= t('created_at') %></th>
      <th><%= t('updated_at') %></th>
    </tr>

    <% @users.each do |user| %>
      <tr>
<!--        <td><%#= link_to user.user_name,[:admin,user] %></td>-->
        <td><%= user.user_name %></td>
        <td><%= user.email %></td>
        <td><%= user.admin? ? "あり":"なし" %></td>
        <td><%= user.created_at %></td>
        <td><%= user.updated_at %></td>
        <td><%= link_to t('edit'), edit_admin_user_path(user) %></td>
        <td><%= link_to t('destroy'), [:admin, user],method: :delete,data:{confirm:I18n.t('confirm')} %></td>
      </tr>
    <% end %>
  </table>
</div>
<%#= paginate @tasks %>
<%= link_to t('new'), new_admin_user_path %>